# Bind Protocol API URL (defaults to production if not set)
BIND_API_URL=https://api-dev.bindprotocol.xyz

# Your Bind Protocol API key (required for prove jobs)
BIND_API_KEY=your-api-key-here

# Optional: Override circuit ID
# CIRCUIT_ID=bind.mobility.basicriskband.v0_1_0

# Optional: DIMO vehicle token ID for real data
# DIMO_TOKEN_ID=12345

# Optional: Verifier org ID to share proofs with
# VERIFIER_ORG_ID=your-verifier-org-id

# For verifier example (shared proof mode)
# BIND_VERIFIER_API_KEY=verifier-api-key-here
# SHARED_PROOF_ID=shared-proof-id-from-prover
