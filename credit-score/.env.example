# Bind Protocol API URL (defaults to production if not set)
BIND_API_URL=https://api.bindprotocol.xyz

# Your Bind Protocol API key (required for prove jobs)
BIND_API_KEY=your-api-key-here

# Optional: Override circuit ID
# CIRCUIT_ID=bind.demo.credit-score.v1
